%h1 用户列表

%table
  %tr
    %th{:style => 'width:80px;'} 用户名
    %th{:style => 'width:200px;'} Email
    %th{:style => 'width:80px;'} 管理员
    %th{:style => 'width:40px;', :colspan => 3}

  - @users.each do |user|
    %tr
      %td= user.username
      %td= user.email
      %td= user.is_admin? ? '是' : '否'
      %td
        = link_to '展示', user, :class => 'button'
        = link_to '修改', edit_user_path(user), :class => 'button'
        = link_to '删除', user, :method => :delete, :data => { :confirm => '确定吗?' }, :class => 'button'

%br

= link_to '新用户', new_user_path, :class => 'button'
