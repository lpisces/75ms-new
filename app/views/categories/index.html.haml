%h1 categories 列表

%table
  %tr
    %th Name
    %th Is parent
    %th Parent
    %th
    %th
    %th

  - @categories.each do |category|
    %tr
      %td= category.name
      %td= category.is_parent
      %td= category.parent_id
      %td= link_to '展示', category
      %td= link_to '修改', edit_category_path(category)
      %td= link_to '删除', category, :method => :delete, :data => { :confirm => '确定吗?' }

%br

= link_to '新 Category', new_category_path
