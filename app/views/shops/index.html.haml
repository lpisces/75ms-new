%h1 shops 列表

%table
  %tr
    %th Name
    %th Url
    %th Description
    %th Logo
    %th
    %th
    %th

  - @shops.each do |shop|
    %tr
      %td= shop.name
      %td= shop.url
      %td= shop.description
      %td= shop.logo
      %td= link_to '展示', shop
      %td= link_to '修改', edit_shop_path(shop)
      %td= link_to '删除', shop, :method => :delete, :data => { :confirm => '确定吗?' }

%br

= link_to '新 Shop', new_shop_path
